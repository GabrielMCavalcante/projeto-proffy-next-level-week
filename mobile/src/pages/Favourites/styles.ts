import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#f0f0f7'
    },

    favourites: {
        marginTop: -40
    },

    spinner: {
        transform: [{ scale: 2 }],
        flex: 1
    }
})